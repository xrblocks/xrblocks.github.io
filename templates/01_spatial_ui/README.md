# Spatial UI

Build a movable spatial card with text, an image, a slider, and buttons.

## Run

Serve the repository and open `/templates/01_spatial_ui/`.

## Controls

Use the buttons to change the message. Drag the slider to change image
brightness. Drag the card to move or resize it.
