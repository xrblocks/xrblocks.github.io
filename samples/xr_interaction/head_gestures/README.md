# Head Gestures

This sample shows completed head nod and shake gestures in a spatial `UICard`.
The same camera-pose pipeline runs on device and in the desktop simulator.

Serve the repository and open
`http://localhost:8080/samples/xr_interaction/head_gestures/?formFactor=desktop`.
