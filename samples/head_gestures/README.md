# Head Gestures

This sample enables completed head nod and shake detection. It displays the
latest gesture and confidence in a small HUD.

The same camera-pose pipeline runs on device and in the desktop simulator. In
the simulator, use mouse look to perform a complete nod or shake motion.

Serve the repository and open
`http://localhost:8080/samples/head_gestures/?formFactor=desktop`.
