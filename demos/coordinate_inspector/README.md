# Coordinate Inspector

Run XR Blocks, then open:

`http://localhost:8080/demos/coordinate_inspector/`

Use WASD to move on the simulator navmesh. Move the mouse to aim the ray. The
overlay shows the current user and scene-mesh hit coordinates in meters.
